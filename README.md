# React Query Devtools broken with ESBuilt

```
npm install
npm start
```

Then go to localhost:8000 and inspect console.
The following error is thrown:

```
Error: No QueryClient set, use QueryClientProvider to set one
```
